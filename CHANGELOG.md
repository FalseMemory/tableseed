# 更新日志 CHANGELOG

所有对外可见的变更按时间倒序记录。格式参考 [Keep a Changelog](https://keepachangelog.com/)。

## [0.4.0] 2026-09-12 · WebUI 2.0：多连接 · 多配置文件 · 两段式入库

### Added 新增
- **数据库连接迁移 config.ini**：多连接管理，页面随时切换；YAML 的 `database` 段兼容保留
- **密码走环境变量**（`password_env`）：YAML/ini 不落明文密码；环境变量缺失时明确报错并给出 setx 写法
- **配置文件多文件工作区**：左栏下拉切换 / 另存为 / 移除，清单存于 config.ini `[workspace]`
- **确认插入（两段式入库）**：生成只在内存预览，点「确认插入」才写库；
  防重复拦截基于**数据内容哈希**（改配置后自动放行）
- **操作日志页**：生成 / 插入 / 查询 / 连接操作全记录；分页、类型与结果筛选、
  关键字搜索、清空；JSONL 落盘跨重启恢复
- **配置编辑器**：表格化编辑 + Navicat 风格单元格编辑器（长内容下方大编辑区）+
  「加载左侧配置」+「? 帮助」（九种组类型参数说明）+ 取值参数单值裸值显示
- **SQL 查询台**：建表语句反射（含库表现有表提示）、查询结果分页、结构化连接入参
- **UI 全面美化**：渐变品牌色、药丸 Tab、统计卡、粘性表头、滚动条、弹窗毛玻璃

### Fixed 修复
- 配置校验错误中文化：pydantic 英文报错映射为中文（20+ 类），YAML 常见语法错误给中文提示
- 连接信息含特殊字符（`%`）无法保存与加载 —— configparser 插值冲突，读写侧均 `interpolation=None`
- 列内联 `PRIMARY KEY`（`varchar(20) PRIMARY KEY`）此前不被识别，主键列被误推断为 const
- 反射建表语句 500：领域异常统一转 400 + 明确 detail；表不存在时列出库中现有表
- 关系图 aggregate 反向边与正向边路径重叠导致文字叠压
- 操作日志详情按钮被 CSS ellipsis 裁掉不可见
- 首次打开页面数据库连接不自动回填

### Changed 变更
- **单条 INSERT 生成全 const**（非主键）：一个值没有推断空间不猜；多条才自动判断类型
- 编辑器取值参数**单值裸值显示**（`["0001"]` → `0001`），解析时自动包回模型结构
- 示例配置分层：`samples/txn.yaml`（纯通用）、`samples/demo_schema.sql`（演示库 DDL）
- e2e 测试改用 `tests/fixtures/txn.yaml`，与用户工作文件彻底解耦

## [0.3.0] 2026-09-11 · M4 完成：split 拆分与生成后自检

### Added 新增
- `split` 拆分传播：分单位整数 + 割点法，Σ子 = 父 零误差守恒；支持 `ratio` 占比
- 生成后自检：配置声明 `invariants` 则生成结束自动逐条求值，违例行连同整行数据列出
- WebUI：关系图（拓扑分层 + 传播模式徽章）、配置编辑器、快速生成（DDL + INSERT → YAML 草稿）

## [0.2.0] 2026-09-11 · M3：aggregate 回填与不变量

### Added 新增
- `aggregate` 两阶段生成：Phase 1 正向传播，Phase 2 反向回填父表汇总字段
- `invariants` 不变量：行级断言 + 跨表断言（复用聚合求值器），WebUI「验证不变量」
- 覆盖策略：`full` / `pairwise`（定向锚定贪心，可复现）/ `sample`，结果带组合覆盖度列

## [0.1.0] 2026-09-11 · M1/M2：内核与 WebUI 闭环

### Added 新增
- 字段分组模型：九种组类型、组是完备划分、组内字段共进退
- 表间关系模型：四要素（基数/存在性/锚点/传播）+ copy/derive/map/free + 拓扑排序
- 1:1 分配策略：follow_parent（按驱动字段分组，保覆盖 + 保一致）/ round_robin / random / weighted
- 条件枚举：组的取值集可随父行变化（`when` 表达式）
- CLI 全家桶（ui / check / plan / verify / gen）+ WebUI 最小闭环 + SQL 查询台
